#!fsharp

#r "nuget: FSharp.Data"
#r "nuget: XPlot.Plotly"
#r "nuget: XPlot.Plotly.Interactive"

open System.IO
open System.Text

open FSharp.Data
open XPlot.Plotly

#!fsharp

let bla = CsvFile.Load(__SOURCE_DIRECTORY__ + "/data/pulsar.csv").Cache()

#!fsharp

bla.Rows
|> Seq.map (fun row -> Seq.map float row.Columns |> Seq.indexed)
|> Chart.Line
